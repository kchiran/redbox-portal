"use strict";
var __values = (this && this.__values)/* istanbul ignore next */ || function (o) {
    var m = typeof Symbol === "function" && o[Symbol.iterator], i = 0;
    if (m) return m.call(o);
    return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
};
Object.defineProperty(exports, "__esModule", { value: true });
var errors = require("./../../../errors");
var getInsertErrorMessageText_1 = require("./../../getInsertErrorMessageText");
/**
 * Replacement handler for replacing parts of the tree that should be equal.
 */
var StraightReplacementNodeHandler = /** @class */ (function () {
    function StraightReplacementNodeHandler(compilerFactory) {
        this.compilerFactory = compilerFactory;
    }
    StraightReplacementNodeHandler.prototype.handleNode = function (currentNode, newNode) {
        /* istanbul ignore if */
        if (currentNode.getKind() !== newNode.getKind())
            throw new errors.InvalidOperationError(getInsertErrorMessageText_1.getInsertErrorMessageText("Error replacing tree!", currentNode, newNode));
        var newNodeChildren = newNode.getChildrenIterator();
        try {
            for (var _a = __values(currentNode.getChildrenIterator()), _b = _a.next(); !_b.done; _b = _a.next()) {
                var currentNodeChild = _b.value;
                var newNodeChild = newNodeChildren.next().value;
                this.handleNode(currentNodeChild, newNodeChild);
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (_b && !_b.done && (_c = _a.return)) _c.call(_a);
            }
            finally { if (e_1) throw e_1.error; }
        }
        /* istanbul ignore if */
        if (!newNodeChildren.next().done)
            throw new Error("Error replacing tree: Should not have new children left over."); // todo: better error message
        this.compilerFactory.replaceCompilerNode(currentNode, newNode.compilerNode);
        var e_1, _c;
    };
    return StraightReplacementNodeHandler;
}());
exports.StraightReplacementNodeHandler = StraightReplacementNodeHandler;

//# sourceMappingURL=StraightReplacementNodeHandler.js.map
