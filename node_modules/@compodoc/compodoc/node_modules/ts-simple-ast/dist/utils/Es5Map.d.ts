export interface Dictionary<T, U> {
    get(key: T): U | undefined;
    set(key: T, value: U): void;
    has(key: T): boolean;
    delete(key: T): void;
    entries(): IterableIterator<[T, U]>;
}
export declare class Es5Map<T, U> implements Dictionary<T, U> {
    private readonly items;
    private readonly propName;
    private itemCount;
    private static instanceCount;
    set(key: T, value: U): void;
    get(key: T): U | undefined;
    has(key: T): boolean;
    delete(key: T): void;
    entries(): IterableIterator<[T, U]>;
    private getIdentifier(key);
    private createIdentifier(key);
}
